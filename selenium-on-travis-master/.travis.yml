language: java

jdk: oraclejdk8

sudo: enabled
os: osx


deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  github-token: $w1Nybb4BO91o4709Hjvdlw
 keep-history: true
  on:
    branch: master
